let numeros = [10, 20, 30];
numeros.push(40);      // agregar
numeros.pop();         // eliminar último
numeros.shift();       // eliminar primero
numeros.unshift(5);    // agregar primero

console.log(numeros);
